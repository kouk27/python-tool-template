# Linux（ネイティブ Docker）でホストの所有者に出力を合わせる場合に使用。
# 次のコマンドで自分の UID/GID を書き出してから docker compose run する:
#   printf "UID=%s\nGID=%s\n" "$(id -u)" "$(id -g)" > .env
# macOS / Windows（Docker Desktop）では自動マッピングのため不要。
UID=1000
GID=1000
